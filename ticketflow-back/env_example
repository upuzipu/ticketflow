# HTTP
HTTP_ADDR=:8080

# PostgreSQL
PG_DSN=postgres://ticketflow:ticketflow@localhost:5432/ticketflow?sslmode=disable

# JWT (min 32 bytes)
JWT_SECRET=change-me-at-least-32-bytes-long-secret!!

# Redis
REDIS_ADDR=localhost:6379

WS_ALLOWED_ORIGINS=localhost:3000,localhost:3005,localhost:8080